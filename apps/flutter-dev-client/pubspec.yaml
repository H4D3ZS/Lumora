name: flutter_dev_client
description: Native Flutter client for Lumora with real-time schema interpretation
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # WebSocket communication
  web_socket_channel: ^2.4.0
  
  # JSON parsing and serialization
  json_annotation: ^4.8.1
  
  # State management (for internal client state)
  provider: ^6.1.1
  
  # Network image caching
  cached_network_image: ^3.3.0
  
  # UI components
  cupertino_icons: ^1.0.6
  
  # Design tokens
  kiro_ui_tokens:
    path: ../../packages/lumora_ui_tokens
  
  # Core schema interpretation
  kiro_core:
    path: ../../packages/kiro_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^3.0.0
  json_serializable: ^6.7.1
  build_runner: ^2.4.6
  mockito: ^5.4.4

flutter:
  uses-material-design: true
