import 'package:flutter/material.dart';
import 'package:flutter_bloc/flutter_bloc.dart';
import 'package:lumora_ui_tokens/lumora_ui_tokens.dart';
import '../bloc/{{featureNameLower}}_bloc.dart';
import '../bloc/{{featureNameLower}}_event.dart';
import '../bloc/{{featureNameLower}}_state.dart';

/// Page widget for the {{featureName}} feature.
/// 
/// This page provides the UI for the {{featureName}} feature and manages
/// its state using BLoC pattern. It automatically loads data on initialization
/// and handles loading, error, and loaded states.
class {{featureName}}Page extends StatelessWidget {
  const {{featureName}}Page({super.key});

  @override
  Widget build(BuildContext context) {
    return BlocProvider(
      create: (context) => {{featureName}}Bloc()..add(const Load{{featureName}}()),
      child: Scaffold(
        appBar: AppBar(
          title: const Text('{{featureTitle}}'),
        ),
        body: BlocBuilder<{{featureName}}Bloc, {{featureName}}State>(
          builder: (context, state) {
            // Show loading indicator while data is being fetched
            if (state is {{featureName}}Loading) {
              return const Center(
                child: CircularProgressIndicator(),
              );
            }

            // Show error message if something went wrong
            if (state is {{featureName}}Error) {
              return Center(
                child: Column(
                  mainAxisAlignment: MainAxisAlignment.center,
                  children: [
                    Icon(
                      Icons.error_outline,
                      color: LumoraColors.error,
                      size: 48,
                    ),
                    const SizedBox(height: 16),
                    Text(
                      'Error: ${state.message}',
                      style: LumoraTypography.bodyMedium.copyWith(
                        color: LumoraColors.error,
                      ),
                      textAlign: TextAlign.center,
                    ),
                  ],
                ),
              );
            }

            // Show content when data is loaded
            if (state is {{featureName}}Loaded) {
              return _buildContent(context, state);
            }

            // Default empty state
            return const SizedBox.shrink();
          },
        ),
      ),
    );
  }

  /// Builds the main content of the page when data is loaded.
  Widget _buildContent(BuildContext context, {{featureName}}Loaded state) {
    return {{{widgetCode}}};
  }
}
